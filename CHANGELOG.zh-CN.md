# 更新日志

本文件记录 MQ Studio 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

[English](CHANGELOG.md)

## [未发布]

应用围绕驱动端口做了重建，所以这不是相对 0.1.3 的变更列表，而是重建后的应用能做什么。
0.1.x 那几个版本早于这次重建、已经删除；这条线上的首个版本会另取版本号。

### 新增

- **同时连接多个集群。** 每个连接有自己的标签页和页面，每个请求都指明它跑在哪个连接上。
- **RocketMQ 完整接入。** 总览、Topic、消费组、消息查询、死信、生产、集群健康，全部读实时
  NameServer。
- **Topic 与消费组操作。** 创建、编辑、删除 Topic；按时间重置消费组位点；把一个组的位点
  克隆给另一个组；直接写单个队列的位点。
- **消息操作。** 按 Key、Tag、时间窗或 msgId 查询；实时跟随一个 Topic；查看消息轨迹；重发
  死信；把一条消息投递给指定消费者并看它的处理结果。
- **集群操作。** 读取 Broker 与 NameServer 的有效配置、查看副本落后多少、按需执行 Broker
  清理、摘除写权限以便停机前排空。
- **访问控制。** RocketMQ 5.3 的用户与授权可读可写；4.x 的 plain_acl 作为只写降级模式保留，
  并在页面上说明它读不回来。
- **告警。** Broker 离线、消费组无实例、堆积、磁盘水位、死信，跨所有已打开的连接求值，
  在标题栏和独立页面呈现，可选桌面通知。
- **能力层。** 每个连接上报端点能做什么，导航据此绘制：端点做不了的页面禁用并说明原因，
  这一族根本没有的概念不绘制。

### 说明

- 目前只有 RocketMQ 可以连接。其余协议的入口是绘制出来但禁用的；RabbitMQ 驱动已经写好，
  但页面还没接。
- 有六项操作被 `rocketmq-admin-go`（本应用依赖的管理库）的缺陷挡着，其中最明显的是消费组的
  创建与编辑。每一项都有一个断言当前行为的测试钉住，库修好即可解锁。

