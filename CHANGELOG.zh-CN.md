# 更新日志

本文件记录 MQ Studio 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

[English](CHANGELOG.md)

## [未发布]

### 新增

- 接入 Apache Pulsar，第四个驱动。主题及其分区与存储方式、其上的命名空间与租户、带积压和
  游标的订阅、消息浏览与实时跟随、发送控制台、Broker、死信主题、角色授权和告警。数据面走
  二进制协议，其余走管理 REST API，两者分别上报：web service 能应答而 broker 端口不通的
  连接仍然可以读取所有页面，并会说明为什么发不了消息。
- 页面按 Pulsar 自己的语义写，而不是把别的中间件的说法翻译过来。这里没有 tag —— RocketMQ
  的生产者放进 tag 的东西，Pulsar 的生产者放进 property，所以发送控制台收集的是 property，
  消息页也按它筛选。订阅是 Broker 保存的游标，没有消费者连着也照样存在，所以「空闲」是正常
  状态而不是消费组消失了。Token 页列的是角色授权而不是账号，因为 Pulsar 授权的是 Token 的
  subject，集群本身不保存任何账号目录。
- 新增「订阅被阻塞」告警，这是其它中间件没有的。未确认消息超过上限后 Broker 会完全停止向该
  订阅投递 —— 只看积压的话和消费者变慢一模一样，但要靠确认消息或调大上限来解决，而不是去动
  消费者。

## [0.0.4] - 2026-09-01

更新流程的完整闭环。新版本现在每次启动都会提示一次，直到被回应为止；而提示它的
那条通知本身，就是安装它的地方。

### 新增

- 有待处理的新版本时，每次启动提示一次；提示会一直留到被回应，而不是几秒后自己
  消失。这份记忆属于本次会话，所以被关掉而没有回应的版本，下次启动还会回来 ——
  「跳过」是唯一让它彻底停下的开关。用户自己按下的检查则直接打开对话框，因为他
  本来就在等这个答案。
- 更新对话框承载了更新的全部 —— 更新内容、下载与进度、以及重启 —— 并且可以从
  标题栏和通知两处打开。安装不再需要专门跑一趟设置页，那里此前是唯一能装的地方。
  发布页在每个阶段都保留入口，因为应用自己换不掉自己的时候，那是唯一的出路。
- 发布说明按 Markdown 渲染，而不是把标记本身显示出来：加粗、链接、分隔线、GitHub
  的 `> [!IMPORTANT]` 提示块，以及跨源码行折行的列表项现在是一条而不是好几条。
  链接交给系统浏览器打开，因为 webview 没有回来的路。渲染器只产出 React 节点、
  不产出 HTML，遇到不认识的语法降级成普通段落 —— 发布说明是远端内容。

### 变更

- 标题栏的更新按钮从「刷新」图标换成向上箭头，并且指向更新本身：有待处理的版本时
  点开对话框、并在提示里写明版本号；没有时仍然是发起一次检查。
- 应用不再自称 RocketMQ 客户端。macOS 包、Windows 文件描述、Linux 桌面项与打包
  元数据、以及窗口描述统一改为「消息队列」，不会随着驱动增加而过期；broker 的名字
  归到 Linux keywords 里，并补上了 Kafka。

### 修复

- 标题栏图标比写下的尺寸大了 22%。它们用 `rem` 写、基准按 13 算，但缩放梯度缩的
  是整个文档而不是根字号，实际生效的是浏览器的 16。现在改用 px，回到 40px 的栏
  当初就是按 28px 设计的那个值。
- 搜索按钮比旁边所有元素都高，标签也比标签页大一号 —— 它用的是 shadcn 的整页默认
  尺寸，而这里不是一整页。
- 命令面板此前固定距顶 96px，在至少 750px 高的窗口里显得偏上。现在居中，代价是
  结果被过滤时输入框会跟着面板高度移动。
- 更新通知在被回应之后仍然留着：跳过一个版本，通知还在说它等着；点了下载，同一条
  通知又从头坐到尾。
- 中文句子如果正好在强调标记之后折行，句中会多出一个空格 —— 拼接时比较的是标记
  本身，而不是读者真正看到的那个字。

### 已知限制

- Pulsar、Redis Stream 与 MQTT 仍在界面上呈现但禁用。
- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于
  清除隔离标记。

## [0.0.3] - 2026-08-31

Kafka 支持，直接走 Kafka 自己的协议，而不是旁边另接一个管理 API。Topic、消费组、
消息与访问控制都已接入；Kafka 本身不报的数字一律留白，不做填充。

### 新增

**Kafka 3.x 与 4.x**

- 通过 Kafka 自身协议连接集群，支持 SASL/PLAIN、SASL/SCRAM 与 TLS。SCRAM 摘要
  单独作为一个字段：SHA-256 与 SHA-512 在 broker 上是两套独立凭据，同一个用户在
  另一种摘要下会认证失败，把这种失败报成「密码错误」是在撒谎。
- 概览、Topic、消费组、消息、发送台、集群、访问控制、配额与告警。
- Topic 展示分区、leader、同步副本与离线副本、副本因子与最小同步副本，行内展开
  可看完整配置文档。支持创建、修改与删除；修改只动明确给出的配置项，不会把用户
  从未见过的设置悄悄重置回默认值。
- 消费组展示分区级与成员级 lag、状态、协调者与分配策略。支持 Kafka 的全部五种
  位点重置 —— 最早、末端、指定时刻、指定 offset、相对偏移 —— 以及把一个组的位点
  复制给另一个组、删除消费组。
- 按最新窗口、offset 区间、时刻或 key 浏览日志，以及实时跟随末端。浏览不加入任何
  消费组、也不提交位点，因此可以安全地对着生产环境使用。
- 发送支持 key、header、指定分区与 acks 级别，并回显消息落在哪个分区的哪个 offset。
- Broker 展示生效配置与日志目录，包括是哪些分区占掉了空间。
- ACL 按主体分组展示，以及集群自己保存的 SCRAM 用户。集群没有配置 authorizer 时，
  两者都会带原因降级，而不是报错。
- 清空 Topic，即 Kafka 自己的截断：现有记录全部不可再读，而位点不回零 —— 停在 900
  的消费者仍在 900，只是已经追平了。
- 把分区迁到别的 broker、看着它迁、以及取消进行中的迁移。Kafka 的迁移没有完成事件 ——
  分区不再上报它就是迁完了 —— 所以页面去读集群，而不是自己宣布成功。旁边是优先副本选举，
  用来把 leader 放回副本列表本来指定的位置。
- 客户端配额：限制挂在「谁在调用」而不是「调用什么」上，可按用户、应用或地址设置，
  也包括没有配额点名时它们各自回落到的默认值。
- 集群页的事务：集群知道哪些事务生产者、每个顶住了什么、顶了多久，以及它是否已经超过了
  协调者承诺中止它的时限。生产者中途死掉导致管道停住，只有这一页看得出来 —— 这种集群
  的其他每一处都是健康的。
- 告警来自分区健康（副本不足、离线、无 leader 分开计数）与消费组 lag。

### 修复

- 清除连接凭据实际上没有清除。此前只按名字删掉了 RocketMQ 的 AccessKey/SecretKey，
  其他家族的密码在「清除」之后仍然留在存储里，下次连接用的还是表单已经声明删掉的
  那一份。这个问题自 RabbitMQ 上线起就存在。
- 连接地址为空时的提示写的是 RocketMQ 的 NameServer，让 Kafka 与 RabbitMQ 的用户去
  找一个自己表单里根本没有的字段。

### 说明

- 有三样东西 Kafka 不报，本次也不编。没有死信页：Kafka 没有 broker 侧死信队列，
  `.DLT` 后缀是 Spring Kafka 的约定而不是 Kafka 的。没有任何速率，因为管理协议里
  就没有。也没有磁盘百分比，因为 Kafka 只报分区占用的字节数，不报承载它的文件系统。

## [0.0.2] - 2026-08-31

支持 RabbitMQ。管理面是完整的，消息面走 AMQP 而不是管理接口的 publish 与 get：
发送会等 publisher confirm，浏览的行为和一个真正的消费者一致。

### 新增

**RabbitMQ 3.x 与 4.x**

- 连接 broker 的 HTTP 管理插件，同时建立 AMQP 数据面连接。该连接在 broker 上以
  `mq-studio: <连接名>` 标识自己，运维可以认出是哪个客户端。
- 概览、队列、Exchange 与 Binding、连接与信道、消息、死信、发送台、节点。
- 队列带完整 arguments：classic / quorum / stream、持久化、TTL、最大长度与
  overflow、死信交换机与路由键、单活消费者。可声明、清空、在队列间搬运、删除。
- Exchange 与 Binding：四种类型外加 alternate exchange；Binding 带路由键与
  arguments，含 headers 交换机的 `x-match`。
- 浏览走 AMQP 而不是管理接口，可按路由键或 header 过滤。队列被原样放回，页面也
  写明浏览的代价：读过的消息回队列时会带上 redelivered 标记。
- 带 confirm 的发送：目标交换机与路由键、mandatory、持久化、优先级、过期时间、
  headers、correlation id、reply-to、content type。没有任何绑定能路由的消息会被
  如实报告为不可路由，而不是假装成功。
- 死信从 `x-death` 头还原：来自哪个队列、为什么被拒、被拒了几次、发生在什么时候。
  可单条或批量重投回原队列或指定目标，也可以直接丢弃。
- 连接与信道，含协议、心跳、prefetch、未确认数与流控状态，并可带原因强制关闭。
- 节点，含内存分解、资源告警、网络分区、broker 自己的健康检查、特性开关，以及
  哪些已弃用特性正在被真正使用。
- 虚拟主机：增删改、默认队列类型、删除保护、tracing，以及连接数与队列数限流。
- 用户与权限：用户与 tag、按虚拟主机的 configure/write/read 正则三元组、主题
  权限、按用户的限流。改一个用户的 tag 不再需要知道他的密码。
- 策略与操作员策略，含优先级、pattern 与 definition，并显示某条队列实际命中了
  哪条策略；运行时参数与全局参数并列其中。
- 定义：导出整个 broker 或单个虚拟主机到文件；导入前先看清它会创建什么。
- Shovel 与 Federation：有哪些、是否在跑、没跑时 broker 自己给的那句话。只读与
  删除 —— 定义里带着对端 broker 的凭据，凭据在离开驱动之前就被抹掉了。
- stream 队列会报告通过流协议连上来的客户端，这些客户端从不出现在队列的 AMQP
  消费者列表里。
- 告警按 RabbitMQ 自己的指标推导：资源告警、网络分区、向水位线的逼近、队列堆积、
  无人消费的队列，以及被 broker 限流的连接。

### 修复

- 告警规则对所有连接都读 RocketMQ 的属性键，于是 RabbitMQ 被拿着它根本不上报的
  指标去衡量，无论 broker 有多糟都一条告警都不会触发。
- 管理面请求无视连接上配置的超时时间，因为底层库不接受 context。broker 一慢，
  页面就可以无限等下去。
- 密码错误被报成「请启用 management 插件」，把人支去重新配置一个本来没问题的
  broker。

### 已知限制

- Kafka、Pulsar、NATS、MQTT 等仍在界面上呈现但禁用。
- Shovel、Federation 与流协议是 RabbitMQ 的插件。没装这些插件的 broker 仍然保留
  对应页面，只是禁用并写明原因。
- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于
  清除隔离标记。

## [0.0.1] - 2026-08-31

重构后 MQ Studio 的首个版本。MQ Studio 是一个消息中间件桌面客户端，围绕驱动端口
组织：对一种消息中间件的支持，是同一套接口背后的一个实现，而不是散落在每个页面里
的假设。

本版本仅支持 RocketMQ，其余协议在界面上呈现但禁用。

### 新增

**架构**

- 驱动端口：一种消息中间件就是一个驱动，实现同一套接口；其上的服务层与桥接层不关心
  回答请求的是哪一族。
- 能力模型：每个连接上报它所连的端点究竟能做什么，界面据此绘制。端点做不了的页面
  禁用并说明原因；这一族根本没有的概念不绘制。
- 多个连接可以同时打开，各自一个标签页和一套页面，每个请求都指明它跑在哪个连接上。

**RocketMQ 4.x 与 5.x**

- 总览、Topic、消费组、消息查询、死信、消息发送、集群健康，全部读取实时 NameServer。
- Topic 与消费组操作：Topic 的创建、编辑与删除；按时间重置消费组位点；把一个组的
  位点克隆给另一个组；直接写单个队列的位点。
- 消息操作：按 Key、Tag、时间窗或 msgId 查询；实时跟随一个 Topic；查看消息轨迹；
  重发死信；把一条消息投递给指定消费者并读回它的处理结果。
- 集群操作：读取 Broker 与 NameServer 的有效配置、查看副本落后多少、按需执行清理、
  摘除 Broker 写权限以便停机前排空。
- 访问控制：RocketMQ 5.3 的用户与授权可读可写；4.x 的 plain_acl 作为只写降级模式
  保留，并在页面上说明它读不回来。
- 告警：Broker 离线、消费组无实例、消息堆积、磁盘水位、死信，跨所有已打开的连接
  求值，在标题栏和独立页面呈现，可选桌面通知。

**支持平台**

- macOS（Apple Silicon 与 Intel）、Windows（x64 与 ARM64）、Linux（x64 与 ARM64，
  提供 .deb、.rpm 与 AppImage）。

### 已知限制

- 目前只有 RocketMQ 可以连接。RabbitMQ 驱动已经写好，但页面还没有建。
- 有四项操作被本应用所依赖的 RocketMQ 管理库的缺陷挡着，其中最明显的是消费组的创建
  与编辑。每一项都有一个断言当前行为的测试钉住，库修好即可解锁。
- macOS 版本尚未使用 Apple 开发者证书签名，磁盘映像内附「首次运行」助手，用于清除
  隔离标记。

### 说明

- 版本号从这里重新开始。0.1.x 那几个构建早于这次重构，已经删除。
