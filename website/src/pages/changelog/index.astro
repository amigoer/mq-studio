---
import ChangelogPage from '@/components/ChangelogPage.astro';
import { releases } from '@/lib/changelog';

const entries = releases('zh');
const latest = entries.find((entry) => !entry.unreleased) ?? entries[0];
---

<ChangelogPage locale="zh" current={latest} isIndex />
